---
ftp:
  host: localhost
  port: 21
  username: username
  passwd: password
  protocol: FTP
directories:
  ftp-source: /current
  store: ./store
  store-buffer: ./store-buffer
  archive: ./archive
playlist:
  path: ./playlist.m3u
  remove-delay: 5
kodi:
  host: localhost
  port: 8080
  protocol: http
  username: username
  passwd: password